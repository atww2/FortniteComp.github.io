import {getSettings,saveSettings} from "./storage.js";import {applyTheme} from "./theme.js";import {$} from "./dom.js";export function init(){const s=getSettings();document.querySelectorAll("[data-choice]").forEach(b=>{b.classList.toggle("active",b.dataset.choice===s.theme);b.onclick=()=>{saveSettings({theme:b.dataset.choice});applyTheme(b.dataset.choice);document.querySelectorAll("[data-choice]").forEach(x=>x.classList.toggle("active",x===b))}});const lang=$("#settings-language"),reg=$("#settings-region"),time=$("#settings-time");lang.value=s.language;reg.value=s.region;time.value=s.timeFormat;lang.onchange=()=>saveSettings({language:lang.value});reg.onchange=()=>saveSettings({region:reg.value});time.onchange=()=>saveSettings({timeFormat:time.value})}