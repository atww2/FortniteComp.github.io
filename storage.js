import {DEFAULTS} from "./config.js";const KEY="fnc:settings";export function getSettings(){try{return {...DEFAULTS,...JSON.parse(localStorage.getItem(KEY)||"{}")}}catch{return {...DEFAULTS}}}export function saveSettings(p){const next={...getSettings(),...p};try{localStorage.setItem(KEY,JSON.stringify(next))}catch{}document.dispatchEvent(new CustomEvent("fnc:settings-changed",{detail:next}));return next}