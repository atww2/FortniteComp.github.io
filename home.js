import {fetchItemShop} from "./shopApi.js";import {getSettings} from "./storage.js";import {ago} from "./time.js";import {$} from "./dom.js";export async function init(){try{const d=await fetchItemShop({language:getSettings().language});$("#stat-items").textContent=d.totalItems;$("#stat-updated").textContent=ago(d.updatedAt)}catch{$("#stat-items").textContent="—";$("#stat-updated").textContent="Unavailable"}}