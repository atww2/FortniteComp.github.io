import {getSettings} from "./storage.js";export function fmtDate(d){const s=getSettings();return new Intl.DateTimeFormat(s.language==="ru"?"ru-RU":"en-US",{day:"2-digit",month:"short"}).format(d)}export function fmtTime(d){const s=getSettings();return new Intl.DateTimeFormat(s.language==="ru"?"ru-RU":"en-US",{hour:"2-digit",minute:"2-digit",hour12:s.timeFormat==="12h"}).format(d)}export function fmtRange(a,b){return `${fmtDate(a)} · ${fmtTime(a)}${b?`–${fmtTime(b)}`:""}`}export function ago(d){const m=Math.max(0,Math.floor((Date.now()-d.getTime())/60000));if(m<1)return"только что";if(m<60)return`${m} мин. назад`;const h=Math.floor(m/60);if(h<24)return`${h} ч. назад`;return`${Math.floor(h/24)} дн. назад`}}